services:
  api:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      AI_INTERVIEW_ROOT: /app
    volumes:
      - hf-cache:/data/hf-cache
    restart: unless-stopped

volumes:
  hf-cache:
